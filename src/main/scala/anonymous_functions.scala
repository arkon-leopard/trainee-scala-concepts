/**
  * Funciones anonimas
  * Son funciones que no tienen un nombre tal cual 
  */

object Anonymous_functions extends App {
    ((a: String) => println(s"Hello $a"))("Fidel") // Funcion anonima llamada automaticamente
}